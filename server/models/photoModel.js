const db = require ('../../db/index.js');

module.exports = {
  read: ({answer_id}) => {
    let queryString = `
    
    `;

    db.query(queryString);
  },

  create: ({answer_id, url}) => {
    let queryString = `
    
    `;

    db.query(queryString);
  },
}